{
  "_comment": "Combined config when you want both abs-mcp and its companion rba-mcp running side by side. Drop into ~/Library/Application Support/Claude/claude_desktop_config.json.",
  "mcpServers": {
    "abs": {
      "command": "uvx",
      "args": ["abs-mcp"]
    },
    "rba": {
      "command": "uvx",
      "args": ["rba-mcp"]
    }
  }
}
